# SC REVIT v0.7.0

本版封存 2026-08-19 已完成實際模型建立驗證的消防支管基線，重點是讓 CAD 主管幾何、撒水頭接管路徑、SVG 路網圖與 Revit 建模使用同一份拓樸計畫。安裝包會同時更新 GUI 與 Revit 2024 DLL。

## 主要更新

- 依 CAD 幾何、交點與連續路徑建立主管及支管 Route Graph。
- 支援多段、轉折主管的分析與路網圖顯示，不再只以單一直線代表主管。
- 當撒水頭可能連到多條主管時，以 CAD 路徑實際連通證據作為最高優先判定。
- 管徑判定依序使用鄰近文字、線段顏色及圖層備援。
- SVG 畫布依 Revit 視圖方位顯示主管、支管、管徑、異徑、三通／四通與未驗證路徑。
- Revit 建模直接執行拓樸計畫，不另外重算接管路線。
- 支管接入使用專案 Pipe Type 的三通 Routing Preference，不再以彎頭加獨立轉接頭模擬三通。
- 正式建立若只有局部失敗，可選擇保留成功部分或全部復原；詳細結果會區分主要失敗與下游影響。

## 建議操作流程

1. 在目前 Revit 視圖確認 CAD Link 可見且已正確對位。
2. 選取主管並開啟 `消防支管建立`。
3. 讀取主管，再框選需要連接的撒水頭。
4. 執行分析，先核對 SVG 路網圖的方向、管徑、異徑及未驗證路段。
5. 執行建立前完整檢查；完成後工具會自動復原測試元素。
6. 確認結果後正式建立。
7. 若顯示部分建立成功，先查看主要失敗位置及受影響灑水頭，再決定保留或全部復原。

## 已知限制

- 僅支援 Revit 2024。
- CAD 模式需要目前視圖中有可見且已對位的 CAD Link。
- 不同專案的 Pipe Type、System Type、管件族群與 Routing Preferences 會影響實際可建立的三通及異徑。
- 少數端點三通可能被 Revit 拒絕。工具會保留完整診斷與成功部分，但該接點下游的灑水頭可能無法連回主管。
- 可直接編輯 SVG 拓樸、無 CAD 統一管徑頁籤及支管自動避梁仍在規劃中。

## 安裝或更新

1. 關閉 Revit 2024 與所有 SC REVIT 視窗。
2. 完整解壓縮 `SC_REVIT_v0.7.0_installer.zip`。
3. 執行 `Install_SC_REVIT.bat`。
4. 安裝完成後重新開啟 Revit 2024。

請勿使用 GitHub 自動產生的 Source code ZIP 安裝。
