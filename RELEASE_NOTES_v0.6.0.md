# SC REVIT v0.6.0

本版將 `main` 已完成的消防支管 CAD 管徑分析、拓樸路網預覽、正式建模、中文診斷及新版 GUI 整合成完整的一鍵安裝／更新版本。安裝包會同時更新 GUI 與 Revit 2024 DLL。

## 消防支管

- 從目前 Revit 視圖中的已對位 CAD 讀取支管路徑與管徑證據。
- 管徑判定優先順序統一為「文字 → 線段顏色 → 圖層備援」。
- 建立唯一拓樸計畫，記錄管段、交點、方向、管徑、T 接、四通與異徑位置。
- SVG 路網圖與 Revit 正式建模共用同一份拓樸計畫。
- SVG 支援縮放與拖曳，並顯示管徑、線粗差異、異徑及待確認項目。
- 支援分段管徑、沿程變徑、主管接入及撒水頭垂管。
- 撒水頭位置保持不變；垂直末段依實際族群 Connector 需求建立。
- 新增建立前完整檢查，完成後自動復原，不污染正式模型。
- 正式建立若局部失敗，可由使用者選擇保留成功部分或全部復原。
- 錯誤結果改以中文摘要、影響範圍及完整詳細資料呈現。

## CAD 點位與預覽

- 自動判斷 CAD／Revit 座標與單位，阻擋異常遠距預覽。
- 預覽改採暫存顯示，重新預覽、取消、完成或關閉工具時自動清理。
- 自動移除 SC REVIT 建立且已無實例的預覽群組類型，不影響使用者群組。

## GUI 與診斷

- 消防支管頁面改為左右分區，開啟工具時自動載入專案資料。
- 分析結果改用獨立視窗，避免壓縮主管、支管與撒水頭操作區。
- 使用者介面移除開發測試用語，統一改為建立前檢查、安全預檢與詳細資料。
- 診斷資料分離為摘要、完整 request、完整 response、拓樸計畫及大型附件，避免摘要長度影響追查。

## 安裝或更新

1. 關閉 Revit 2024 與所有 SC REVIT 視窗。
2. 完整解壓縮 `SC_REVIT_v0.6.0_installer.zip`。
3. 執行 `Install_SC_REVIT.bat`。
4. 安裝完成後重新開啟 Revit 2024。

## 已知限制

- 本版只支援 Revit 2024。
- 消防支管 CAD 管徑模式需要可見且已正確對位的 CAD 連結。
- 不同專案的族群 Connector、System Type、Pipe Type 與 Routing Preferences 仍須先執行建立前完整檢查。
- L／U／雙 L 型複合主管、可直接編輯的 SVG 拓樸、自動避梁及無 CAD 統一管徑頁籤仍在規劃中，未列入本版。
