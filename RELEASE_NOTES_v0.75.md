# SC REVIT v0.75

發布日期：2026-08-21

此版封存目前消防支管 CAD 智慧管徑、SVG 拓樸核對與 Revit 建模可用基線。重點是把目前已能工作的拓樸計畫、SVG 顯示與建模契約固定下來，作為後續 UI/UX、複合主管與避梁功能的基準。

## 主要更新

### 消防支管 CAD 智慧管徑

- CAD Route Graph 改以目前視圖可見的 CAD 路徑、主管幾何與撒水頭候選路徑建立拓樸計畫。
- 支管候選以 CAD 路徑是否實際連到指定主管作為最高優先證據。
- 管徑判斷維持工程優先順序：CAD 文字、線段顏色、圖層備援與未標註預設。
- 支管沿程變徑依工程規則重算，不再把主管四通本體的 DN100 轉支管尺寸誤當成額外變徑。

### SVG 路網核對與有限修正

- SVG 改為畫布式檢視，支援縮放、拖曳、選取管段後顯示可用修正。
- 以管段為主要修正對象；接頭與異徑配件依管段管徑變化自動更新，避免使用者直接修改配件造成建模契約失效。
- 修正後 SVG 會立即重繪，並維持固定畫布範圍，避免多次修改造成整體圖面縮放漂移。
- 減少大面積文字卡片遮擋，以管徑、長度與判定依據作為主要顯示資訊。

### Revit 建模與驗證

- 建模端以同一份拓樸計畫為基準，不再自行重新推算管徑、四通或異徑配置。
- 正式建立前會執行必要安全檢核；正式建立成功後保留模型結果。
- 目前驗證基線中，已確認計畫範圍內管段、接頭、四通、異徑與撒水頭連通可建立。
- 局部失敗時以中文說明原因、受影響元素與可復原方式。

## 已知限制

- 複合主管、L 型／U 型／雙 L 型主管的全圖自動判斷仍需更多實案驗證。
- SVG 編輯目前限定管段管徑修正；大幅拓樸重排、手動畫線或接頭手動替換尚未開放。
- 自動避梁尚未列入本版正式功能。
- 無 CAD 底圖的統一管徑舊版模式尚未整理成獨立頁籤。

## 安裝

1. 關閉 Revit 2024 與所有 SC REVIT 視窗。
2. 完整解壓縮 `SC_REVIT_v0.75_installer.zip`。
3. 執行 `Install_SC_REVIT.bat`。
4. 重新開啟 Revit 2024。
